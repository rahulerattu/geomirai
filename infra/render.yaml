services:
  - type: web
    name: geomirai-app
    env: python
    rootDir: app
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - fromDotEnv: true